// import { create } from "zustand";
// import { userStoreI } from "@/@types/userStore.type";

// const useUserStore = create<userStoreI>()((set)=>{
//     user: null,
//     isLoggedIn:false,
//     login:()=>
// })

// import { userStoreI } from "@/@types/userStore.type";
// import { create } from "zustand";


// const useUserStore = create<userStoreI>()((set) => ({
//   user: "",
//   isLoggedIn: false,
//   login: (userData) => set(() => ({ user: userData, isLoggedIn: true })),
//   logout: () => set(() => ({ user: null, isLoggedIn: false })),
//   updateUser: (updateData) =>
//     set((state) => ({ user: { ...state.user, ...updateData } })),
// }));
